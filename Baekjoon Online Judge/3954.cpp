#include <iostream>
#include <stack>
#include <string>
using namespace std;

int t;
int M, S, I;
string prog, input;

struct interprit{
	int iid, pid;
	char mem
};

int main() {
	cin >> t;
	while (t--) {
		cin >> M >> S >> I;
		cin.ignore();
		getline(cin, prog);
		getline(cin, input);
		cin.ignore();
	}
	return 0;
}